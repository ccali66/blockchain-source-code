export declare namespace EncryptDecryptPDF {
    class PDFProcessor {
        password: String;
        username: String;
        constructor(password: String, username: String);
        decrypt(orgfilepath: string, newfilepath: string): Promise<unknown>;
        encrypt(orgfilepath: string, newfilepath: string): Promise<unknown>;
        private process;
    }
}
